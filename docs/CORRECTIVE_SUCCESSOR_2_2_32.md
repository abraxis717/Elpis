# Elpis2.2.32 corrective successor authority

Elpis2.2.31 is immutable failed hosted-main qualification evidence.

Exact sealed commit:

`90467893822422b24a88a449afdccf0e9dd80cf3`

Observed hosted failures:

- CI workflow run `36123410633`: workflow-definition failure before job creation.
- platform-matrix run `36123409522`: workflow-definition failure before job creation.
- reference-runtime run `36123411680`: development verifier rejected `tools/__pycache__/release_git.cpython-311.pyc` generated by its own Python invocation.
- inference-native-r0 and Component attribution succeeded on the same sealed commit.

The CI/platform startup failures are caused by unquoted YAML plain-scalar `run:` values containing `--only-binary=:all:` followed by whitespace. The trailing colon forms YAML `: ` syntax and prevents workflow job materialization.

The 2.2.32 corrective scope is intentionally limited to:

1. Advance current release metadata from 2.2.31 to 2.2.32.
2. Encode hash-locked pip commands as YAML block scalars.
3. Disable Python bytecode generation in reference-runtime hosted verification.
4. Launch `tools.release_orchestrator` as a package module from `tools/full_release.py`.
5. Delegate GitHub push authentication explicitly to the already-authenticated external `gh auth git-credential` provider while retaining sealed Git config and URL-rewrite defenses.
6. Add focused regressions for those mechanics.

No 2.2.31 tag, GitHub Release or PyPI artifact is to be created. The 2.2.31 sealed manifest remains immutable historical failed-release evidence.
